/**
 * Алгоритм - это последовательность действий, направленная на решение конкретной задачи
 *
 * Существуется понятие сложности алгоритма - O(n), O(n!), O(log2n)
 * Данная функция опеределяет то, насколько эффективно (т.е. быстро) алгоритм решает задачу
 *
 * Сложность алгоритма определяется по самому наихуджешму случая
 *
 */
